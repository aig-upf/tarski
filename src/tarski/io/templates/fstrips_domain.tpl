(define (domain {domain_name})
    (:requirements {requirements})
    (:types
        {types}
    )

    (:constants
        {constants}
    )

    (:predicates
        {predicates}
    )

    (:functions
        {functions}
    )

    {derived}

    {actions}
)