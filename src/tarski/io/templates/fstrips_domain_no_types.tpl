(define (domain {domain_name})
    (:requirements {requirements})
    (:constants
        {constants}
    )

    (:predicates
        {predicates}
    )

    (:functions
        {functions}
    )

    {derived}

    {actions}
)