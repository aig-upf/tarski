(define (problem {problem_name})
    (:domain {domain_name})

    (:objects
        {objects}
    )

    (:init
        {init}
    )

    (:goal
        {goal}
    )

    {constraints}
    {domain_bounds}
    {metric}
)

